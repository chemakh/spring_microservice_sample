server:
  port: 8787


spring:
  application:
    name: serviceServer
  cloud:
    config:
      uri: http://localhost:1212

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}