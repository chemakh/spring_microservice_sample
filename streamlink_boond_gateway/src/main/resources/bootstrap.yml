server:
  port: 8989

spring:
  application:
    name: gatewayServer
  cloud:
    config:
      uri: http://localhost:1212

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}